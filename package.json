{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --parallel",
    "buildui": "lerna run libbuild --stream",
    "story": "yarn lerna run story --stream"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
